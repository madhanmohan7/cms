import 'package:flutter/material.dart';


const oYellow = Color(0xFFd8ee12);
const oYellowAccent = Colors.yellowAccent;

const oRed = Color(0xFFFA0E0E);
const oRedDark = Color(0xFFFF0000);

const oBlue = Color(0xFF184bff);
const oBlueDark = Color(0xFF176EF9);
const oBlueLight = Color(0xFFd0d9ff);
const oBlueSky = Color(0xFF00C1D9);
const oBlueGradient = LinearGradient(
  colors: [Color(0xFF184BFF), Color(0xFF0E2D99)],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const oBlueGradient1 = LinearGradient(
  colors: [Color(0xFF184BFF), Color(0xFF0E2D99)],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);

const oBlueGradient2 = LinearGradient(
  colors: [Color(0xFF184BFF), Color(0xFF00C1D9)],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);

const oBlueGradient3 = LinearGradient(
  colors: [Color(0xFF0E2D99), Color(0xFF00C1D9)],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);

const oRedGradient = LinearGradient(
  colors: [Color(0xFFFA0E0E), Color(0xFFFFB800)],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);

const oRedGradient1 = LinearGradient(
  colors: [Color(0xFFFA0E0E), Color(0xFFFFB800)],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const oBlack = Color(0xFF000815);
Color oBlackOpacity = Color(0xFF000815).withOpacity(0.5);
const oBlackLight = Color(0xFF1C2129);

const oGreen = Color(0xFF66CC00);
const oGreenDark = Color(0xFF006400);
const oGreenGradient = LinearGradient(
  colors: [Color(0xFF66CC00), Color(0xFF339900)],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const oGreenGradient1 = LinearGradient(
  colors: [Color(0xFF66CC00), Color(0xFF339900)],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);

const oOrange = Color(0xFFFFB800);

const oPink = Colors.pink;

const oWhite = Color(0xFFFFFFFF);

LinearGradient oWhiteGradient = LinearGradient(
  colors: [Color(0xFFFFFFFF), Color(0xFFF0F0F0).withOpacity(0.5)],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);

LinearGradient oWhiteGradient1 = LinearGradient(
  colors: [Color(0xFFFFFFFF), Color(0xFFF0F0F0).withOpacity(0.5)],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const oGrey = Color(0xFFACA7A7);

const oTransparent = Colors.transparent;


LinearGradient oTransparentGradient = LinearGradient(
  colors: [Colors.transparent, Color(0xFF000815)],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

LinearGradient oTransparentGradient1 = LinearGradient(
  colors: [Colors.transparent, Color(0xFF000815).withOpacity(0.35)],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);
